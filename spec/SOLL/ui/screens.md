# UI (P0)

- Dashboard (/ui)
- Templates list + create (/ui/forms, /ui/forms/new)
- Template detail (/ui/forms/:templateId) incl. versions table + publish buttons
- Entities list (/ui/entities)
- Entity detail (/ui/entities/:entityId) showing JSON + state buttons
